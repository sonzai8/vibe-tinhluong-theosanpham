version: "3.9"

services:
  postgres:
    image: postgres:15-alpine
    container_name: plywood_postgres
    restart: always
    environment:
      POSTGRES_DB: plywood_db
      POSTGRES_USER: payroll
      POSTGRES_PASSWORD: payroll123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: